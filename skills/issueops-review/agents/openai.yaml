interface:
  display_name: "IssueOps Review"
  short_description: "Run and record the adversarial review of an IssueOps plan or diff"
  default_prompt: "Use $issueops-review to run and record an independent review of the selected cycle plan or diff, preserving verdict freshness and following the bounded correction loop and user-authorized scope."
