interface:
  display_name: "IssueOps Verify"
  short_description: "Verify the sealed diff without touching a single file"
  default_prompt: "Use $issueops-verify to validate the sealed diff using its evidence-reuse rules, execute missing or invalidated checks, perform the required review and readiness checks, and continue to the approved endpoint."
