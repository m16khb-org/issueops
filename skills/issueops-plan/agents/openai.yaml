interface:
  display_name: "IssueOps Plan"
  short_description: "Read the constraints, write the plan, get it reviewed, hand off the implementation"
  default_prompt: "Use $issueops-plan to prepare the reviewed plan and canonical worktree before the user chooses the current session, a new session in that worktree, or hold. Follow the skill for direct preparation and preserve any explicitly requested execution mode."
